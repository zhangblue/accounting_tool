[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sea-orm-migration = { version = "1.1.20", features = ["sqlx-postgres", "runtime-tokio-native-tls"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
async-trait = "0.1"
